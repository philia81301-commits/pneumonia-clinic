# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前**必讀**；收工時**必更新**。本檔只放交接必需的精簡資訊，詳細脈絡放 Obsidian（`pneumonia-clinic/專案工作流程.md`）。

## ⏯️ 目前做到哪

**階段一（資料查證）與階段二（規則表）已完成。** 七份資料檔全部附出處與抓取日期，⛔ 待補項目全數清空。
規則引擎 `src/engine.js` ＋ `src/rules.json` 已建立並通過驗證：**419,328 種輸入組合零死角、10 個門診情境全對**。

## 🚦 目前狀態

- 需求：✅ 已凍結（規格卡 status: confirmed，revisions: 3）
- 階段一 資料查證：✅ 完成（`data/01`–`data/07`）
- 階段二 規則表：✅ 完成並通過測試（`node src/engine.test.js`）
- 階段三 HTML 報告：❌ 未開始
- 階段四 PPTX：❌ 未開始
- 階段五 衛教單張：❌ 未開始

## ➡️ 下一步

1. **HTML 報告**（單檔可離線）：內嵌 `rules.json` 與 `engine.js` 做互動決策器；
   免疫橋接說明節**必須置於所有效力數字之前**；每個數字附出處與抓取日期
2. **補接種流程圖**：必須由 `rules.json` 長出來，與決策器答案一致（已有測試可比對）
3. **PPTX**：一頁一訊息、張數不設上限；含**免疫橋接教學模組 7 頁**
4. **衛教單張**：A4 單頁可列印，全白話、無血清型編號
5. 交付前依規格卡驗收條件逐條核對，特別是**用語紅線逐句掃過**

## ⚠️ 注意事項

- **跨電腦約定**：兩台電腦都放 `C:\projects\pneumonia-clinic\`，開工前 `git pull`、收工後 `git push`。**不要放進 OneDrive／Google 雲端硬碟**（.git 會被同步搞壞，醫院電腦也掛不了 G:）
- **PCV21 是成人專用疫苗**，血清型組成與幼兒型 PCV 不同，涵蓋率**不可四支放同一基準直接比大小**
- PCV20 / PCV21 的證據多為**免疫原性橋接（OPA GMT）**而非臨床終點試驗，寫效力時必須標明證據等級，不要與 PPV23／PCV13 的臨床 VE 混為一談
- 排除項（明確不做，勿自行加回）：自費價格數字、成本效益／NNV 試算、用台灣 IPD 重算涵蓋率、幼兒常規接種政策主線
- 但**公費資格條件要寫**（那是政策內容，不等於價格）
- `.pptx` 已在 `.gitignore` 擋掉，產出後記得另外複製到 OneDrive

## 🕐 最後更新

- 時間：2026-08-07（階段一、二完成）
- 更新者：Claude Code（Opus 5）@ DESKTOP-LVSV9Q5
- Git push：✅ 已推
