# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前**必讀**；收工時**必更新**。詳細脈絡放 Obsidian（`pneumonia-clinic/專案工作流程.md`）。

## ⏯️ 目前做到哪（2026-08-09 更新）

**專案已完成，這次是收尾。** 做了兩件事：

1. **兩項規格偏離經使用者確認可接受**（衛教單張改雙面兩頁、免疫橋接模組首頁引用 45.56%）。
   `acceptance.py` 新增 `accepted()` 函式，把已確認的偏離印成 ✓ 並附理由，不再列為待辦。
   驗收現在是 **11 條全過、0 項待辦、exit 0**。

2. **`data/01`–`04` 的待查清單 9 項全部結案**，並因此更正了產出裡的一個實質錯誤。

★ **這次抓到的錯：36% 原本被寫成「曾接種率」，是錯的。**
疾管署 2026-07-28 公告原文是「65 歲以上長者肺炎鏈球菌疫苗接種**完成率**已由政策實施前的
**31%** 提升至 **36%**」。簡報、報告、`data/01`、`data/03` 四處已更正並補上 31% 對照點。
順帶查出 12% 的真正出處是**內科學誌 2025;36(6)**，不是 `data/03` 原本寫的「疾管署新聞稿」。
三個數字的分工與引用紅線已寫進 agents.md 工作約定。

### 待查結案的重點結果

| 項目 | 結果 |
|---|---|
| PPV23 是否含 6A | **不含，只含 6B**（FDA 標籤逐字）。矩陣原本就對，未改 |
| 台灣 PCV21 仿單 | 與美國 PI **完全一致**。兩段式適應症：**IPD 22 型（多 15B，來自 15C 交叉反應）／肺炎 21 型** |
| PCV21 台灣藥證 | **衛部菌疫輸字第001319號**「肺普息」，發證 **2026-04-23** |
| Menactra 台灣藥證 | **查無**。台灣是 Menveo（腦寧安，MenACWY-CRM，000968號），**不受此限制** |
| 限制延伸 PCV20/21？ | **查無官方文件延伸**，僅針對 PCV13，不推論 |
| Zostavax | **2025-10-07 自請註銷**，台灣已無活性減毒帶疹疫苗 |
| 縣市補助／NIDSS 血清型 | 查無官方資料，結案不追（理由已寫進 data 檔）|

**B 表沒有結構性變動**——原待查假設「Zostavax 若退市則 B 表要簡化」其實不成立：
帶狀疱疹本來就在 A 表（Shingrix），B 表只有 MMR 與水痘。合併施打表維持 **9 種疫苗**。

## 🚦 目前狀態

| 項目 | 狀態 |
|---|---|
| 需求規格卡 | ✅ confirmed，revisions: 3 |
| 資料查證 `data/01`–`07` | ✅ 完成，**待查清單全數結案**，全附出處與查詢日 |
| 規則表＋決策引擎 | ✅ v1.2.0，測試 419,381 項零失敗 |
| 互動式 HTML 報告 | ✅ **86.9 KB** 單檔可離線；新增 12%／31%→36% 定義對照表 |
| PowerPoint 簡報 | ✅ **56 張**，版面／出處／一致性／OOXML 四關全過 |
| 病人衛教單張 | ✅ **雙面兩頁**，本次**未動** |
| 總驗收 | ✅ 11 條全過，**0 項待辦**，exit 0 |
| OneDrive `.pptx` | ✅ 已覆蓋為最新版（SHA256 比對相同）|

一鍵重建全部產出：

```bash
node src/engine.test.js && node src/build.js && node src/deck.js
```

QA 四支（簡報檔被 PowerPoint 開著時 `deck.js` 會改寫帶時間戳的備援檔）：

```bash
PYTHONUTF8=1 python src/deck_qa.py output/台灣成人肺炎鏈球菌疫苗簡報.pptx
PYTHONUTF8=1 python src/consistency.py
PYTHONUTF8=1 python src/acceptance.py
```

## ➡️ 下一步

**沒有未完成的工作項目。** 專案處於可交付狀態。若要再往下做，可選：

1. 把 scratchpad 的 `preview.py` 搬進 `src/`（這台沒有 LibreOffice，它是目前唯一能從 `.pptx`
   讀回實際座標做目視 QA 的工具，長期要用就該收進版控）
2. 依實際演講回饋微調簡報（目前 56 張，未實際試講過）

## ⚠️ 注意事項

### 環境限制（本次有更新）

- ✅ **cdc.gov.tw 現在抓得到了**，先前記錄的「一律 403」已不成立。但有兩個坑：
  - 頁面上的 `/File/Get/<id>` 回傳的是 **PDF 檢視器外框 HTML**，不是 PDF。
    真檔在外框 HTML 裡的 `/Uploads/<uuid>.pdf`
  - 官方 PDF 是**掃描影像、無文字層**，`pypdf` 抽不到字，要用 `fitz` 算圖後判讀
- **食藥署藥證用開放資料查最準**：`https://data.fda.gov.tw/data/opendata/export/36/csv`
  （ZIP，約 10MB，解開 44MB CSV，8.3 萬列）。有註銷狀態／註銷日／有效日期／適應症全文。
  ⚠️ 不要用 `exportDataList.do?method=openData&InfoId=36`，那個回傳 Swagger UI 頁面
- ⚠️ **opengovtw.com 的藥證資料是舊的**：Zostavax 在該站寫「有效期限 2023-12-15」，
  官方資料是有效日期 2028-12-15 但已於 2025-10-07 自請註銷。**一律以食藥署開放資料為準**
- **這台機器沒有 LibreOffice**，無法把投影片算圖做目視 QA。替代是 `deck_qa.py`（幾何＋CJK 字寬估算）
- `pubmed.ncbi.nlm.nih.gov` 會擋 CAPTCHA，改用
  `https://www.ebi.ac.uk/europepmc/webservices/rest/search?query=EXT_ID:<PMID>&resultType=core&format=json`
- 簡報檔被 PowerPoint 開著時寫入會 EBUSY，`deck.js` 會改寫 `_備援_YYYYMMDD-HHMM.pptx`

### 版面容量（本次踩到）

簡報的頁尾與表格儲存格**沒有多少餘裕**。這次補台灣藥證註記時撐破兩處
（S52 頁尾 0.42" > 0.34"、S50 表格列 1.01" > 0.43"），`deck_qa.py` 有擋下來。
**要補充說明優先寫進 `note()` 備忘稿**，那裡沒有長度限制。

### 已確立的鐵律（寫在 agents.md 開頭）

**醫療政策一律以疾管署／衛福部／健保署公告為準**；國際指引與期刊須標「非疾管署公告內容」。
**同名指標必須連母體、年份、計算定義一起引**（36% 事件的教訓）。

### 列印衛教單張的設定（2026-08-09 實測，未變動）

紙張為 **A4∩Letter 交集 210 × 279.4mm**，兩種紙匣都能直接印。
仍建議**關掉「頁首及頁尾」**（開著會再吃掉上下各約 12mm）；邊界選「預設」，不要選「無」。

### 跨電腦約定

兩台電腦都放 `C:\projects\pneumonia-clinic\`，開工前 `git pull`、收工後 `git push`。
**不要放進 OneDrive／雲端硬碟**。

## 🕐 最後更新

- 時間：2026-08-09
- 更新者：Claude Code（Opus 5）@ DESKTOP-LVSV9Q5
- Git：✅ 已 commit 並 push（最新 `7d55ea2`）
- OneDrive 備份：✅ `.pptx` 已覆蓋為 56 張最新版（SHA256 比對相同）
