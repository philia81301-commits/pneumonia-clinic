# 交接檔（handoff.md）

> 任何 Agent、任何電腦接手前**必讀**；收工時**必更新**。詳細脈絡放 Obsidian（`pneumonia-clinic/專案工作流程.md`）。

## ⏯️ 目前做到哪

**五個階段完成四個半。** HTML 報告、53 張簡報都已完成並通過全部 QA；病人衛教單張**內容完成但版面超出單頁**。
今日依使用者提供的官方公告原文做了**兩次實質更正**（見注意事項）。

## 🚦 目前狀態

| 項目 | 狀態 |
|---|---|
| 需求規格卡 | ✅ confirmed，revisions: 3 |
| 資料查證 `data/01`–`07` | ✅ 完成，全附出處與查詢日 |
| 規則表＋決策引擎 | ✅ v1.1.0，測試 419,378 項零失敗 |
| 互動式 HTML 報告 | ✅ 65.7 KB 單檔可離線，瀏覽器實測無錯誤 |
| PowerPoint 簡報 | ✅ 53 張，版面／出處／結構／一致性四關全過 |
| 病人衛教單張 | ⚠️ **內容完成，版面約 2.3 頁 A4，需壓縮至單頁** |
| 總驗收 | ❌ 未做 |

一鍵重建全部產出：

```bash
node src/engine.test.js && node src/build.js && node src/deck.js
```

QA 三支（簡報檔被 PowerPoint 開著時 `deck.js` 會自動改寫 `_新版.pptx`）：

```bash
PYTHONUTF8=1 python src/deck_qa.py output/台灣成人肺炎鏈球菌疫苗簡報.pptx
PYTHONUTF8=1 python src/consistency.py
```

## ➡️ 下一步

1. **衛教單張壓縮至 A4 單頁**（目前約 683mm ≈ 2.3 頁）。建議取捨：常見疑問 6 則刪為 4 則、
   移除「新疫苗效果怎麼證明」那則（對病人偏深）、三句帶走重點併入頁尾
2. **總驗收**：依規格卡「✔ 驗收條件」逐條核對三份產出
3. **`.pptx` 複製到 OneDrive**（依約定不進 git，目前雲端無備份）

## ⚠️ 注意事項

### 今日兩次實質更正（都是我先前判讀錯誤，已修）

1. **對象二（三）的 5 年追加**：原依新聞稿附表寫成「任何 65 歲以上、兩劑、間隔 5 年即可追加」，
   **範圍過寬**。公告原文限定「**先前為 19–64 歲 IPD 高風險對象**」，且必須**同時**滿 65 歲、間隔滿 5 年
   （三條件 AND）。一般 65 歲以上完整接種兩劑者視為已完成、不再追加。
2. **IPD 高風險是 5 項不是 6 項**：公告編號 1–5，第 5 項將「惡性腫瘤者」與「器官移植者」併為同一項，
   且限「**一年內**」接受免疫抑制劑或放射治療者。先前誤拆為 6 項。

### 已確立的鐵律（寫在 agents.md 開頭）

**醫療政策一律以疾管署／衛福部／健保署公告為準**。國際指引與期刊須標「非疾管署公告內容」；
藥廠新聞稿、媒體不得作為政策或效力陳述依據。官方文件互有出入時以**致醫界通函**為準。

### 環境限制

- **這台機器沒有 LibreOffice**，無法把投影片算圖做目視 QA。替代方案是 `deck_qa.py`
  （幾何＋CJK 字寬估算），能抓溢框／超界／重疊／表格切字／缺出處，但**不等於真的看過**
- **cdc.gov.tw 對自動抓取一律回 403**，官方 PDF 無法直接下載。`data/01` 中的
  「參、實施對象」與「伍、疫苗轉換之銜接原則」是**使用者提供的逐字原文**，已註明取得方式
- `pubmed.ncbi.nlm.nih.gov` 會擋 CAPTCHA，改用 `https://www.ebi.ac.uk/europepmc/webservices/rest/search?query=EXT_ID:<PMID>&resultType=core&format=json`
- 簡報檔被 PowerPoint 開著時寫入會 EBUSY，`deck.js` 已自動改寫 `_新版.pptx` 不中斷建置

### 跨電腦約定

兩台電腦都放 `C:\projects\pneumonia-clinic\`，開工前 `git pull`、收工後 `git push`。
**不要放進 OneDrive／雲端硬碟**。

## 🕐 最後更新

- 時間：2026-08-08
- 更新者：Claude Code（Opus 5）@ DESKTOP-LVSV9Q5
- Git push：✅ 已推
