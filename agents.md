# 台灣成人肺炎鏈球菌疫苗政策比較（專案藍圖）

> 本檔為跨 Agent 通用的專案藍圖（AGENTS.md 開放標準）。任何 Agent 的每個 session 都應先讀本檔＋`handoff.md`。
> **需求已經過 RDQ 訪談與使用者確認**，規格見 `rdq/RDQ-spec-pneumococcal-vaccine-policy-20260807.md`（status: confirmed）。請勿重複詢問已確認事項。

## 專案簡介

比較 **PPV23 / PCV13 / PCV20 / PCV21** 四支肺炎鏈球菌疫苗在**成人**族群的血清型涵蓋率與保護效力，結合台灣肺炎與 IPD 流行病學現況，產出給**醫療專業人員**的互動式 HTML 報告（內建接種建議決策器）與 PowerPoint 簡報，另附一張給病人的白話衛教單張。

**資料一律取自公開文獻與官方資料**（PubMed、美國 ACIP、台灣疾管署與食藥署），**不使用任何病人資料**。

## 關鍵時程

- 專案啟動：2026-08-07（RDQ 需求確認完成）
- 交付期限：未設定

## 目標與路線圖

- [x] 階段一：資料查證 —— `data/01`–`07` 七份，全附出處與查詢日
- [x] 階段二：**單一規則表** `src/rules.json` ＋ 決策引擎 `src/engine.js`（測試 419,378 項零失敗）
- [x] 階段三：互動式 HTML 報告（單檔 65.7 KB、可離線、含決策器與自動生成流程圖）
- [x] 階段四：PowerPoint 簡報 **53 張**（含免疫橋接教學模組 7 頁）
- [x] 階段五：A4 病人衛教單張 —— **維持雙面兩頁**，兩面各約 234／239mm，可容忍 Letter 紙匣與瀏覽器頁首頁尾
- [ ] 階段六：三份產出交付前總驗收（依規格卡驗收條件逐條檢查）
- [ ] 待辦：`.pptx` 複製一份到 OneDrive 備份（依檔案放置約定，不進 git）

## 資料夾結構

```
C:\projects\pneumonia-clinic\
├── agents.md                    # 本檔，專案藍圖
├── handoff.md                   # 交接檔
├── .gitignore
├── rdq\
│   └── RDQ-spec-pneumococcal-vaccine-policy-20260807.md   # 已確認的需求規格卡
├── data\                        # 查證來源摘錄 01–07，全部附出處與查詢日
├── src\
│   ├── rules.json               # ★ 單一真相來源（公費資格、間隔、警示、ACIP 對照、來源位階）
│   ├── engine.js                # 決策引擎（純函式，瀏覽器與 Node 共用）
│   ├── engine.test.js           # 窮舉測試：node src/engine.test.js
│   ├── report.template.html     # HTML 報告樣板（含注入標記）
│   ├── leaflet.template.html    # 病人衛教單張樣板
│   ├── build.js                 # 建置：注入 rules/engine → output（含衛教單張術語檢查）
│   ├── deck.js                  # 簡報產生器：node src/deck.js
│   ├── deck_qa.py              # 版面＋出處稽核（無 LibreOffice 環境的替代 QA）
│   ├── consistency.py           # 跨產出數字一致性與用語紅線掃描
│   └── serve.js                 # 本機預覽伺服器（僅開發用）
└── output\                      # 產出物（*.pptx 不進 git，見下）
```

## 同步層級（本專案初始化至第三層級）

| 層級 | 平台 | 位置 | 讀取時機 |
|------|------|------|---------|
| L1 | 本地（`C:\projects\pneumonia-clinic\`，不放雲端同步資料夾） | `agents.md`＋`handoff.md` | 每個 session |
| L2 | GitHub（**跨電腦同步唯一管道**） | philia81301-commits/pneumonia-clinic（**私有**） | 指定時 |
| L3 | Obsidian | `pneumonia-clinic/專案工作流程.md` | 有需要時 |

## 檔案放置約定

| 東西 | 放哪 | 理由 |
|------|------|------|
| HTML 報告、衛教單張、規則表、查證摘錄 | 本 repo（git） | 純文字、需版控、無敏感資料 |
| PowerPoint 產出（`.pptx`） | 產生在 `output\`，另複製一份到 OneDrive | 二進位檔不適合 git；依全域檔案分工鐵律，Office 產出走 OneDrive |
| 病人資料 | **不存在** | 本專案不使用任何病人資料 |

## 工作約定

### ★ 來源位階鐵律（醫療政策內容）

**政策陳述一律以台灣官方公告為準：疾管署、衛福部、健保署。** 依序為：

1. **台灣官方公告**（致醫界通函、衛福部新聞稿、健保署公告、官方接種須知）→ 公費資格、間隔規定、適應症認定**只能**引這一層
2. 台灣學會共識與本土期刊 → 臨床判斷與本土流病，**不是政策依據**
3. 國際指引與期刊（ACIP、CDC Yellow Book、Lancet…）→ 引用時**必須標明「非疾管署公告內容」**，且與台灣規定並列、不可混寫
4. 藥廠新聞稿、媒體、衛教網站 → **不得**作為政策或效力陳述的依據

- 官方文件互有出入時，**以給醫事人員的正式函文為準**，並把差異標出來
- 政策數字一律回**官方原文**核對，不接受新聞摘要或搜尋結果的轉述
  （已發生過：新聞稿附表漏掉「先前為 19–64 歲 IPD 高風險對象」限定，導致判定範圍過寬）
- 每個政策數字附**出處連結與查詢日期**

### 其他

- 任何 Agent、任何電腦：**開工先讀 `handoff.md`，收工必更新 `handoff.md`**
- **每個引用數字都要附出處（PMID 或官方公告連結）與抓取日期**，不得出現無法溯源的數字（這是規格卡的硬性驗收條件）
- **決策器與流程圖必須共用同一份規則表**，不得各自實作
- 免疫功能不全族群的建議需單獨列出，不與糖尿病、吸菸等一般慢性病族群併為同一列
- **有數列可畫的內容一律畫成圖**（趨勢用折線、比較用柱狀、對照關係用矩陣），不要只寫成文字條列。
  圖表用原生格式（pptxgenjs `addChart()`／HTML 內嵌），不貼截圖；圖上的數字一樣要溯源
- **疫苗語意色**：`PPV23 #a9c9d4`／`PCV13・PCV15 #6ba3b5`／`PCV20 #2e7f9b`／`PCV21 #0f4c5c`
  （世代由舊到新、色階由淺到深），共有型用中性 `#5f7783`。
  定義在 `src/deck.js` 的 `V{}` 與 `report.template.html` 的 `--pcv*`，**兩處必須同值**（`consistency.py`【4】會擋）。
  這幾色只用於**資料呈現**（圖表、矩陣、表格的疫苗欄），不挪作一般裝飾色
- **衛教單張的列印容錯**：兩面內容各不得超過 **245mm**。可用高度 269mm，
  但瀏覽器若開著「頁首及頁尾」會再吃掉上下約 24mm。改完版面要用瀏覽器實測再交付，
  **不要為了塞版面而縮字級**（讀者是 50 歲以上）
- 修改共用檔案前先讀最新內容，避免覆蓋其他 Agent 的變更
- 所有回應與文件使用繁體中文
